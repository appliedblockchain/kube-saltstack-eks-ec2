apiVersion: v1
kind: Service
metadata:
  name: {{ name }}-svc
  labels:
    app: {{ name }}
spec:
  selector:
    app: {{ name }}
  ports:
  - port: {{ port }}
    targetPort: {{ port }}
  {% if public_access %}type: LoadBalancer{% endif %}
