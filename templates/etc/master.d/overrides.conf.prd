saltenv: prd
worker_threads: 10

file_roots:
  base:
    - /srv/salt/ext
  prd:
    - /srv/salt/ext
    - /srv/salt/environments/prd/states

pillar_roots:
  base:
    - /srv/salt/environments/base/pillars
  prd:
    - /srv/salt/environments/prd/pillars